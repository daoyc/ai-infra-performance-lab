# LLM 推理术语表

本文件用于打通当前阶段最重要的推理链路和指标语言。  
要求：每个词都用你自己的话解释，不抄定义；每个词下面都写出它和你现有 `CPU / runtime / 系统性能` 经验的对应关系。

## 当前已完成进度

从已有学习记录看，你已经完成了“实验环境与入口链路”的第一轮打通：

- 已在云端 GPU 环境上部署 `vLLM`
- 已完成 `Qwen2-7B-Instruct` 模型下载与完整性校验
- 已跑通 benchmark 脚本入口
- 已准备好 Nsight Systems 采集入口

这说明当前真正缺的不是“怎么部署”，而是：

- 用自己的语言解释推理链路
- 把 `TTFT / TPOT / throughput / memory` 真正转成你的指标语言
- 把已有部署经验和你现有 `CPU / runtime / 系统性能` 能力建立对应关系

## decoder-only

我的解释：

- 待补充。

和现有经验的对应关系：

- 它和我已有的哪类 runtime / pipeline / 资源利用分析经验相通，待补充。

## prefill

我的解释：

- 待补充。

和现有经验的对应关系：

- 它更像哪类“初始化阶段 / 首次计算阶段”的性能问题，待补充。

## decode

我的解释：

- 待补充。

和现有经验的对应关系：

- 它更像哪类“迭代式持续执行”的性能问题，待补充。

## KV cache

我的解释：

- 待补充。

和现有经验的对应关系：

- 它和缓存复用、访存路径、内存占用之间的关系，待补充。

## TTFT

我的解释：

- 待补充。

和现有经验的对应关系：

- 它更接近“首次响应时延”的哪类系统指标，待补充。

## TPOT

我的解释：

- 待补充。

和现有经验的对应关系：

- 它更接近“稳定阶段单位产出效率”的哪类性能指标，待补充。

## 推理指标语言速记表

| 指标 | 我当前的理解 | 它主要反映什么 | 常见混淆点 |
| --- | --- | --- | --- |
| `TTFT` | 待补充 | 待补充 | 不要和整体吞吐混为一谈 |
| `TPOT` | 待补充 | 待补充 | 不要和单次请求总时长混为一谈 |
| `throughput` | 待补充 | 待补充 | 吞吐高不等于单请求体验一定好 |
| `memory` | 待补充 | 待补充 | 显存占用和性能提升不总是正相关 |

## 最小推理链路说明

请在本周补出一版最小口头说明，至少覆盖：

1. 请求进入模型后发生什么
2. prefill 和 decode 的本质区别
3. KV cache 为什么重要
4. 为什么不能只看吞吐判断推理性能

## 已有实验链路入口

当前已经具备的入口包括：

- `vLLM --version` 或 Python import 验证
- `huggingface-cli` / `hf-transfer` 模型下载链路
- 基础 `benchmark.py` 执行入口
- `nsys profile` 采集入口

后续术语学习和实验观察都应该围绕这条已跑通的链路展开，而不是再回到环境搭建阶段。
