# 当前开放问题

本文件只保留当前最重要的 3 个问题。  
规则：

- 永远只保留 3 个最卡的问题
- 问题解决后，要么删除，要么标记为已解决
- 不记录泛泛问题，只记录阻塞当前实验推进的问题

## Question 1

- 问题：
  - 当前 benchmark 脚本实际输出的指标里，哪些字段分别对应 `TTFT`、`TPOT`、整体 throughput 和显存占用？
- 为什么卡住我：
  - 我已经跑通了 benchmark 入口，但如果不能把输出字段和指标语言严格对应，后续实验对比就会停留在“跑了命令”，还不能进入真正的性能分析。
- 我准备怎么验证：
  - 先固定一组最小 benchmark，逐项核对输出字段和含义，再把映射关系回写到 `docs/llm-inference-lexicon.md` 与 `benchmarks/inference-baseline.md`。

## Question 2

- 问题：
  - `prefill` 和 `decode` 在当前 `vLLM + Qwen2-7B-Instruct + 4090 24G` 实验链路里，分别会主导哪些可观测现象？
- 为什么卡住我：
  - 如果不能把链路阶段和实际指标变化对应起来，我就无法从“会部署”升级到“会解释和分析推理性能”。
- 我准备怎么验证：
  - 通过控制输入长度、输出长度和并发，对比不同场景下的延迟与吞吐变化，形成第一版阶段性解释。

## Question 3

- 问题：
  - 在当前单卡实验里，下一步最值得优先对比的变量顺序应该是什么：`batch size`、`concurrency`，还是 `input/output length`？
- 为什么卡住我：
  - 当前实验入口已经有了，但如果变量扩散太快，会导致前几轮实验无法形成稳定结论。
- 我准备怎么验证：
  - 先固定模型、版本和硬件，只做 3 类变量中的单变量对比，并把“现象 -> 指标变化 -> 假设”记入实验台账。

## 已解决问题

- `vim` 编辑中文乱码：
  - 临时方案：设置 `encoding / fileencoding / termencoding` 为 `utf-8`
  - 长期方案：在 `~/.vimrc` 中固化 `utf-8` 相关编码配置
